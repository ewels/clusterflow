/*
ChIP-Seq test pipeline
================
Test pipeline for ChIP-seq data. Takes fastq files as input, then runs bowtie, to map, samtools to sort and index, picard to remove duplicates, and phantompeaktools to get cross-correlation statistics. Also runs fastQC on the fastq files.  
*/
#fastqc
#bowtie
	#samtools_sort_index
		#bedtools_bamToBed
			#bedToNrf
		#picard_dedup
			#samtools_sort_index
				#phantompeaktools_runSpp
					#deeptools_bamCoverage
					#deeptools_bamFingerprint

