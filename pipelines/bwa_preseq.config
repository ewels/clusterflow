/*
------------------------
BWA to Sorted Pipeline
------------------------
This pipeline takes FastQ files as input, runs BWA to align, sorts
the alignments using samtools sort and indexes using samtools index.
Preseq lc_extrap is then run on the alignments. The module requires
a bwa genome reference.
*/


@require_reference bwa

#bwa
    #samtools_sort
        #samtools_index
            #preseq